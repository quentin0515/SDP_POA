#include <iostream>
#include <vector>

#include "interval.h"

using namespace std;

//constructor
Interval::Interval(int _len, int _start, int _end) : len(_len), 
            start(_start), end(_end) {}
